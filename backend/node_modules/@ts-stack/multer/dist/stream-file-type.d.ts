import { Transform } from 'node:stream';
import { AnyFn } from './types.js';
export declare class FileType extends Transform {
    #private;
    constructor();
    fileTypePromise(): Promise<{
        ext: string;
        mime: string;
    } | null>;
    _transform(chunk: any, _: any, cb: AnyFn): void;
    _flush(cb: AnyFn): void;
    protected finish(cb: AnyFn): void;
}
//# sourceMappingURL=stream-file-type.d.ts.map